export enum RequestPrfx {
  direct = "/direct/entry",
  login = "/login/checkUser",
  upload = "/upload_file",
}

export enum PathTypesPrfx {
  "test123" = "Tests result Partial",
  // "תוצאות חלקיות" = "Tests result Partial",
  // "תוצאות מלאות" = "Tests result",
  // "תרבית" = "Tests result - tarbit",
}
